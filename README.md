Requirements.txt contains the libraries you will need to install

Will also need to install Ollama (version dependent on PC/MAC specifications and whether you are using Linux/Windows/MacOS):
https://ollama.com/

